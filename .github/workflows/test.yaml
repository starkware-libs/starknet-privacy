name: Test

on:
  workflow_call:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: foundry-rs/setup-snfoundry@v3
        with:
          starknet-foundry-version: "nightly-2026-02-03"
      - uses: software-mansion/setup-scarb@v1
        with:
          scarb-version: "2.15.1"

      - name: Run tests
        run: scarb test -w
